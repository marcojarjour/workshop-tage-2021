
# APP-OF-APPS

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tracing
  labels:
    group: app-of-apps
    feature: tracing
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: in-cluster
  source:
    repoURL: https://github.com/bygui86/workshop-tage-2021
    targetRevision: master
    path: infrastructure/100_gitops/tracing
  destination:
    name: in-cluster
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
