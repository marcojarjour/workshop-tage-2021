apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grpc-apps
  labels:
    feature: grpc
  # annotations:
  #   argocd.argoproj.io/sync-wave: "2"
spec:
  project: in-cluster
  source:
    repoURL: https://github.com/bygui86/workshop-tage-2021
    targetRevision: feature/wt21
    path: infrastructure/90_apps/20_grpc
  destination:
    name: in-cluster
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=background
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
