apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: http-client-db
spec:
  selector:
    matchLabels:
      app: http-client-db
  namespaceSelector:
    matchNames:
      - apps
  endpoints:
    - port: metrics
      interval: 15s
      scrapeTimeout: 14s
